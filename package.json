{
  "name": "homebridge-otodo-vav",
  "version": "0.1.0",
  "description": "Homebridge plugin for Otodo VAV thermostats (auth + refresh ready)",
  "keywords": [
    "homebridge-plugin",
    "otodo",
    "vav",
    "thermostat"
  ],
  "license": "MIT",
  "author": "Surfysamy",
  "engines": {
    "node": ">=18.0.0",
    "homebridge": ">=1.6.0"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "config.schema.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "clean": "rimraf dist",
    "prepack": "npm run clean && npm run build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "homebridge": "^1.11.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0"
  }
}
